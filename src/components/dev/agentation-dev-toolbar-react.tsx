/**
 * Agentation dev toolbar - React island for annotating UI elements
 * and generating structured feedback for AI coding agents.
 * Only rendered in development mode via client:only="react".
 */
import { Agentation } from "agentation";

export default function AgentationDevToolbar() {
  return <Agentation />;
}
